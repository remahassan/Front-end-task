import { GetCopmponentDirective } from './get-copmponent.directive';

describe('GetCopmponentDirective', () => {
  it('should create an instance', () => {
    const directive = new GetCopmponentDirective();
    expect(directive).toBeTruthy();
  });
});
